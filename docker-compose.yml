version: '3.8'

services:
  privacy-browser:
    build: .
    container_name: privacy-search-browser
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
